name: CI
on:
  workflow_dispatch:
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
       - uses: actions/checkout@v4
       - name: create a text file
         run: echo " hello world." > hello.txt
     
